# /etc/urender.conf
# Configuration file for uRender

# uRender UPnP Friendly Name (default is 'uRender').
URENDER_NAME=${CFG_NET_URENDER_SYSTEMNAME}

# Interface to listen to (default is eth0).
# Ex : URENDER_IFACE=eth1
URENDER_IFACE=wlan0

# Port to listen to (default is random from IANA Dynamic Ports range)
# Ex : URENDER_PORT=49200
URENDER_PORT=${CFG_NET_URENDER_PORT}

# Port to listen for Telnet connections
# Ex : URENDER_TELNET_PORT=1337
URENDER_TELNET_PORT=

# Directories to be shared (space or CSV list).
# Ex: USHARE_DIR=/dir1,/dir2
URENDER_DIR=${CFG_PUBLIC_DRIVE}:

# Use to override what happens when iconv fails to parse a file name.
# The default uRender behaviour is to not add the entry in the media list
# This option overrides that behaviour and adds the non-iconv'ed string into
# the media list, with the assumption that the renderer will be able to
# handle it. Devices like Noxon 2 have no problem with strings being passed
# as is. (Umlauts for all!)
#
# Options are TRUE/YES/1 for override and anything else for default behaviour
URENDER_OVERRIDE_ICONV_ERR=

# Enable Web interface (yes/no)
ENABLE_WEB=

# Enable Telnet control interface (yes/no)
URENDER_ENABLE_TELNET=no

# Use XboX 360 compatibility mode (yes/no)
ENABLE_XBOX=

# Use DLNA profile (yes/no)
# This is needed for PlayStation3 to work (among other devices)
ENABLE_DLNA=

